# SWEETSPOT_DASHBOARD_PARITY_SPEC — AMENDMENT 1 (v2, Aug 9 2026)

**Context:** the parity base (client ssCautionLines verbatim mirror + six-bucket
HOLDER READ validation) SHIPPED Aug 8 at `c05b482` — giveback cut came back flat
~24% and market-priced across all six buckets, so the read stands time-validated.
This amendment covers the six items surfaced in the Aug 9 session (classifier
semantics, synthesis gap, band UX). No code before PM go.

## A. GATE LINE
One row on every WNBA card, hierarchy order:
`GAP .19 ✓ · LDR .59 → WATCH-ONLY · LEAD: earned · TR: hot`
Renders from existing row/snapshot fields. "FADE COUNTRY" label only when
leader wp < .400; otherwise WATCH-ONLY.

## B. CELL READ v2
One plain-English sentence composing the gate line into an expectation. One
vocabulary (see C), every rate labeled trailer-WINS or lead-HOLDS, power tag
required, time-conditioned per the three-form kernel. Template:
"WATCH-ONLY — real gap (.19) but the leader is elite (.59). Lead reads earned:
leads like this HOLD ~60% vs trailers this much better [small sample]. Good
until Q4; silent inside 5:00."

## C. CLASSIFIER DEFINITIONS CONTRACT (agent-integrity fix — ships FIRST)
Pinned definitions block included verbatim in BOTH narration prompt templates
AND backing the display translation table (elite-contract pattern):
- lead_class = MARGIN COMPOSITION. STRUCTURAL = paint+FT >= 60% of margin;
  VOLATILE = 3s+mid >= 60%; MIXED = neither; EVEN = margin <= 2 — too thin to
  classify (NOT "evenly composed").
- fuel = LEADER REGRESSION CHANNELS. EARNED = no heat markers, no takeaway
  feed; TRANSIENT = heat and/or takeaway.
- trailer temp = ABSOLUTE bands (cold <45 / warm 45-55 / hot >55), all quarters.
- leader bands (green/orange/red) = PERIOD-ADJUSTED (Q1 54/61 -> Q4 60/69),
  transience detection only — never a "cold" read.
Prose precedence: margin <= 2 -> thin-margin phrasing; else fuel leads,
composition qualifies. Consumer audit checklist: both narration templates, push
copy assembly, trap kernel (lead_class === 'STRUCTURAL' reference), verdict
strip, EK chip / matchup sheet.

## D. TAP-TO-LEGEND
Tapping any band chip shows that quarter's thresholds inline.

## E. SEASON eFG REFERENCE
`58.3 (szn 51.2)` beside live eFG. Display-only (PM delta lens); no gate change.

## F. TRAILER-COLD -> BLUE
Trailer cold renders blue. Audit pass: green renders only where green means
calm/stable (leader band); kill the cold-renders-green collision.

## Fixtures
- Definitions-block pin: agent prompt contains all four definitions verbatim.
- CELL READ composition cases: fade-country / watch-only / thin-margin /
  Q4-suspended.
- Translation-table render pins (VOLATILE -> "variance-built", EVEN -> "margin
  too thin to read", etc.).
- Cold-blue swatch pin.

## Rollout order
C (agent integrity) -> B + A -> D / E / F.
