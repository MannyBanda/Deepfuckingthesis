# PAPER LEDGER — zero-dollar instinct calls (newest on top)

Companion to `research/2026-08-15_elite_elite_PREREG.md`. Manny's discretionary
off-stream calls, logged at the window, resolved nightly. Never pooled with the
bets table. Target ~20-30 calls before any read.

Format per row:
- **date | matchup | side @ live price | hypo stake | window (period/clock, score) | thesis one-liner | RESULT | hypo P&L**

---

*(no calls logged yet — retro-entries are not permitted; a call counts only if logged at or before the window)*
