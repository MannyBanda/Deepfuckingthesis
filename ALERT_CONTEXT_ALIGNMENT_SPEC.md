# ALERT CONTEXT ALIGNMENT SPEC (ACA) — shipped Aug 6, 2026

Origin: LA@MIN row-1197 post-mortem. Architectural idea: ONE COPY SOURCE, THREE SURFACES
(mechanical push → narration → dashboard); the model narrates AROUND pinned facts, never
mediates them. Full traced spec + PM approvals in the Aug 6 session; phases shipped in order.

## P0 (commit 0a66b7a)
- B1: `_pushBody = push.body + ...` rebuild dropped the D-13 escalation breadcrumb when the
  team-ctx line rendered. Fixed.

## P1 (0a66b7a) — trap parity + T+0 FACT block
- `ssCautionLines(ft, row, regime)` is THE pinned-copy source: STICKY / EARNED-LEAD CAUTION /
  CHANNEL NOTE / STRUCTURAL-LEADER TRAP (dashboard copy kernel mirrored, fixture-pinned).
- Stage-1b mechanical push appends the full fuel/temp/caution/trap block at T+0 — zero model latency.

## P2 — JUICE intelligence
- `trailer_lane` BOOLEAN stamped at fire (OVERRIDE_LANE rule from internal def-A tiers,
  `_kk` cache pattern, NULL-degrading) + schema migration (init run in prod).
- Real tier capLines (placeholder retired), lane-aware; "Staged adds = one position vs cap."
- F5: period ≥4 swaps the fire-state cushion for the market-honest line + Q4 re-entry recipe.
- Open-position ADD line via isolated `bets` SELECT (result='PENDING'); depends on entry-time logging.

## P3 — narration v2
- B2: legacy inline fire narration (opus-4-8, 110-word contract) DELETED — V2 tail sweep
  (ssCallNarration, fable-5 `output_config.effort:'high'` first, opus fallback attempt ≥2)
  is the only narration path.
- Prompt contract v2 (both builders): 5-element decision brief — THE SHAPE (grading language,
  must reflect pinned traps) → CASE FOR (provenance tags [PRIOR n]/[2026 season]/[live]
  mandatory) → CASE AGAINST (argued) → WATCH CONDITIONS (concrete levels) → THE PRICE
  (breakeven framing). 210-word cap.
- Context feed: schedule-inflation band in composeTeamContext (def-A derived, band+number
  only, never tier labels — KILLER_FLAG_SPEC §7); XGB in snapState ONLY at extremes
  (≥.90/<.30) with interpretation; MC line dropped for trailing reads (margin-echo).
- Regime tripwire: gatherer reads learnings.ss_json.regime (db-api extraction mirrored);
  INVERTED suspends all numeric (2026) lines in cautions + client HOLDER READ; traps are
  framework rules and always render.

## P4 — companions (open)
- Pre-registered ledger cut: review-fired games reaching Q4 down 1–3, conditional conversion
  vs market (would earn F5's line its numbers).
- EXIT_P holder-side alerts: next major spec, builds on P2 position awareness.
- T+0 FACT block regime plumb (fire-time) — currently sweep-only; monthly-pulse revisit covers copy.

Fixtures: decision-support 115/115, squeeze 69/69, ss_narration 50/50 (contract-v2 repins).
